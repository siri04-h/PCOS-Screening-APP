// Flip this to false once Member 3's backend is running and BASE_URL in
// src/api/client.js points at it. While true, every screen works against
// realistic in-memory fake data instead of making real network calls —
// useful for demoing/reviewing the UI before the backend exists.
export const MOCK_MODE = false;
